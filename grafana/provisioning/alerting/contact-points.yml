apiVersion: 1

contactPoints:
  - orgId: 1
    name: Default
    receivers:
      - uid: slack-notifications
        type: slack
        settings:
          url: $SLACK_WEBHOOK_URL
        disableResolveMessage: false
